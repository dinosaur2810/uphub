# Copy to .env.jenkins (gitignored) — Jenkins REST API credentials
# Username is your Jenkins login id, NOT the token label "jenkins".

JENKINS_URL=http://localhost:8080
JENKINS_USER=tagayanfinal
JENKINS_API_TOKEN=your-api-token-here
