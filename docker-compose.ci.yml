# Jenkins CI overlay: alternate host port to avoid conflicts with local dev (8081).
services:
  web:
    ports:
      - "8082:80"
    environment:
      APP_BASE: /UpHub
      APP_URL: http://localhost:8082/UpHub
